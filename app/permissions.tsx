import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import { Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useNova } from '../src/state/NovaProvider';
import { colors, radii } from '../src/ui/theme';
export default function PermissionsScreen(){const {capabilities,requestPermission}=useNova();return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><View style={s.header}><View><Text style={s.eyebrow}>NOVA / SAFETY</Text><Text style={s.title}>Permissions</Text></View><Pressable style={s.close} onPress={()=>router.back()}><Ionicons name="close" size={20} color={colors.text}/></Pressable></View><Text style={s.subtitle}>Every native surface is opt-in. A denied permission never blocks chat, files metadata, or the offline agent.</Text>{Object.entries(capabilities).map(([name,value])=><View key={name} style={s.row}><View style={s.icon}><Ionicons name={name==='camera'?'camera-outline':name==='microphone'?'mic-outline':name==='notifications'?'notifications-outline':'images-outline'} size={20} color={colors.primary}/></View><View style={s.copy}><Text style={s.label}>{name.replace(/([A-Z])/g,' $1')}</Text><Text style={s.meta}>{value}</Text></View><Pressable onPress={()=>requestPermission(name as keyof typeof capabilities)}><Text style={s.manage}>{value==='granted'?'Granted':'Request'}</Text></Pressable></View>)}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.bg},content:{padding:20,gap:12},header:{flexDirection:'row',justifyContent:'space-between'},close:{width:38,height:38,borderRadius:12,backgroundColor:colors.surface,alignItems:'center',justifyContent:'center'},eyebrow:{fontSize:10,letterSpacing:1.5,color:colors.primary,fontWeight:'700'},title:{color:colors.text,fontSize:32,fontWeight:'800',marginTop:4},subtitle:{color:colors.muted,fontSize:15,lineHeight:22,marginBottom:8},row:{padding:15,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',gap:12},icon:{width:40,height:40,borderRadius:12,backgroundColor:colors.surface2,alignItems:'center',justifyContent:'center'},copy:{flex:1},label:{color:colors.text,fontWeight:'800',textTransform:'capitalize'},meta:{color:colors.muted,fontSize:12,marginTop:4},manage:{color:colors.primary,fontWeight:'800'}});
