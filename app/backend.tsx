import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import { Pressable, ScrollView, StyleSheet, Switch, Text, TextInput, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { useBackend } from '../src/backend/BackendProvider';
import { colors, radii } from '../src/ui/theme';

export default function BackendScreen() { const { config, health, tools, outbox, saveConfig, refresh, discoverTools, flush } = useBackend(); const statusColor = health.status === 'healthy' ? colors.success : health.status === 'offline' ? colors.muted : colors.purple; return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><View style={s.header}><View><Text style={s.eyebrow}>NOVA / BACKEND</Text><Text style={s.title}>Backend</Text></View><Pressable style={s.close} onPress={() => router.back()}><Ionicons name="close" size={20} color={colors.text}/></Pressable></View><Text style={s.subtitle}>Optional remote services for sync, tool discovery, job dispatch, and event ingestion. Offline mode remains the default.</Text><View style={s.health}><View style={[s.statusDot,{backgroundColor:statusColor}]}/><View style={s.copy}><Text style={s.healthTitle}>{health.status}</Text><Text style={s.meta}>{health.message}{health.latencyMs ? ` · ${health.latencyMs} ms` : ''}</Text></View><Pressable onPress={() => void refresh()}><Ionicons name="refresh-outline" size={20} color={colors.primary}/></Pressable></View><Text style={s.section}>Connection</Text><View style={s.card}><Text style={s.label}>Mode</Text><View style={s.switchRow}><Text style={s.meta}>{config.mode === 'remote' ? 'Remote backend enabled' : 'Offline-only execution'}</Text><Switch value={config.mode === 'remote'} onValueChange={(value) => void saveConfig({ ...config, mode: value ? 'remote' : 'offline' })} trackColor={{true:colors.primary,false:colors.border}} thumbColor={colors.text}/></View><Text style={s.label}>Base URL</Text><TextInput value={config.baseUrl} onChangeText={(baseUrl) => void saveConfig({ ...config, baseUrl })} placeholder="https://api.example.com" placeholderTextColor={colors.muted} autoCapitalize="none" autoCorrect={false} style={s.input}/><Text style={s.label}>Workspace ID</Text><TextInput value={config.workspaceId} onChangeText={(workspaceId) => void saveConfig({ ...config, workspaceId })} style={s.input}/><View style={s.switchRow}><View style={s.copy}><Text style={s.label}>Allow remote tools</Text><Text style={s.meta}>Remote descriptors are shown only when enabled.</Text></View><Switch value={config.allowRemoteTools} onValueChange={(allowRemoteTools) => void saveConfig({ ...config, allowRemoteTools })} trackColor={{true:colors.primary,false:colors.border}} thumbColor={colors.text}/></View></View><Text style={s.section}>Operations</Text><View style={s.actions}><Pressable style={s.action} onPress={() => void discoverTools()}><Ionicons name="search-outline" size={18} color={colors.primary}/><Text style={s.actionText}>Discover tools</Text></Pressable><Pressable style={s.action} onPress={() => void flush()}><Ionicons name="cloud-upload-outline" size={18} color={colors.primary}/><Text style={s.actionText}>Flush outbox ({outbox.length})</Text></Pressable></View>{tools.length > 0 && <><Text style={s.section}>Remote registry</Text>{tools.map((tool) => <View key={tool.id} style={s.tool}><View style={s.copy}><Text style={s.label}>{tool.name}</Text><Text style={s.meta}>{tool.description}</Text><Text style={s.badge}>{tool.risk} · v{tool.version}</Text></View><Ionicons name={tool.enabled ? 'checkmark-circle' : 'pause-circle'} size={20} color={tool.enabled ? colors.success : colors.muted}/></View>)}</>}</ScrollView></SafeAreaView> }
const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.bg},content:{padding:20,gap:12},header:{flexDirection:'row',justifyContent:'space-between'},close:{width:38,height:38,borderRadius:12,backgroundColor:colors.surface,alignItems:'center',justifyContent:'center'},eyebrow:{fontSize:10,letterSpacing:1.5,color:colors.primary,fontWeight:'700'},title:{color:colors.text,fontSize:32,fontWeight:'800',marginTop:4},subtitle:{color:colors.muted,fontSize:15,lineHeight:22},health:{padding:15,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',gap:10},statusDot:{width:10,height:10,borderRadius:5},copy:{flex:1},healthTitle:{color:colors.text,fontWeight:'800',textTransform:'capitalize'},meta:{color:colors.muted,fontSize:12,lineHeight:18,marginTop:3},section:{color:colors.muted,textTransform:'uppercase',fontSize:11,letterSpacing:1.4,fontWeight:'800',marginTop:8},card:{padding:15,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,gap:8},label:{color:colors.text,fontWeight:'800'},switchRow:{flexDirection:'row',alignItems:'center',justifyContent:'space-between',gap:10},input:{borderRadius:10,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surface2,color:colors.text,padding:11},actions:{flexDirection:'row',gap:8},action:{flex:1,padding:13,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',justifyContent:'center',gap:7},actionText:{color:colors.text,fontWeight:'800',fontSize:12},tool:{padding:14,borderRadius:radii.md,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',gap:10},badge:{color:colors.primary,fontSize:11,marginTop:5,textTransform:'capitalize'}});
